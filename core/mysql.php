<?php

function insere(string $entidade, array $dados) : bool
{
    $retorno = false;

    foreach ($dados as $campo => $dados) {
        $coringa[$campo] = '?';
        $tipo[] = gettype($dado) [0];
        $$campo = $dado;
    }

    $instr
}
?>